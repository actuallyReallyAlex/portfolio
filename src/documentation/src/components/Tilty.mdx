---
name: Tilty
route: /components/Tilty
menu: Components
---

import { Playground, PropsTable } from 'docz'
import Tilty from '../../../components/Tilty'
import Reposier from '../../../components/works/Reposier'

# Tilty

An element that becomes 3D and tilts on hover. Uses [react-tilt](https://www.npmjs.com/package/react-tilt).

## Example

---

<Tilty />

---

## Properties

<PropsTable of={Tilty} />

## Portfolio Item

A portfolio item Tilty will be slightly less enlarged. The `options` prop will be as follows:

`options={{ max: 15, perspective: 2000, scale: 1.05 }}`

<Playground>
  <Tilty portfolioItem item={<Reposier />}/>
</Playground>

## All Options

| **Option**  | **Default**                     | **Description**                                                  |
| ----------- | ------------------------------- | ---------------------------------------------------------------- |
| reverse     | false                           | Reverse the tilt direction.                                      |      
| max         | 35                              | Max tilt rotation (degrees).                                     |
| perspective | 1000                            | Transform perspective, the lower the more extreme the tilt gets. |          
| scale       | 1                               | 2 = 200%, 1.5 = 150%, etc ...                                    |
| speed       | 300                             | Speed of the enter / exit transition.                            |
| transition  | true                            | Set a transition on enter / exit.                                |
| axis        | null                            | What axis should be disabled. Can be X or Y.                     |
| reset       | true                            | If the tilt effect has to be reset on exit.                      |
| easing      | "cubic-bezier(.03,.98,.52,.99)" | Easing on enter / exit.                                          |
